# Sanitized source snapshot

- Maintained guide claim: run adjacent shell tests with `bash <path>`.
- Executable evidence: a fixture command exits zero.
- Old guide claim: the same fixture command, with an old date, still exits zero.
- Conflicting raw note: proposes a different command but is not canonical.
- Restricted source: represented by an opaque reference; sentinel substance and
  restricted title/location are supplied only inside the isolated disclosure case.
